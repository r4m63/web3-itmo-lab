configure.ac:

AC_INIT([myproject], [1.0], [you@example.com])
AM_INIT_AUTOMAKE([-Wall -Werror foreign])
AC_PROG_CC
AC_CONFIG_FILES([Makefile])
AC_OUTPUT

Makefile.am:

bin_PROGRAMS = myproject
myproject_SOURCES = main.c

---

autoscan

Он создаст configure.scan — можешь посмотреть его и переименовать в configure.ac, если хочешь стартовать с шаблона.

aclocal
autoconf
automake --add-missing

После этого появятся скрипты configure и шаблоны Makefile.in

Сборка проекта:

./configure
make


===============
aclocal
autoconf
automake --add-missing
./configure
make download-deps
===============


